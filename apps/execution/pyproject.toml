[project]
name = "execution"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.128.8",
    "sqlalchemy>=2.0",
    "psycopg[binary]>=3.1",
    "pydantic-settings>=2.11.0",
    "ib_async>=1.0",
    "alembic>=1.13",
]
[tool.fastapi]
entrypoint = "app.main:app"
